management:
  endpoints:
    web:
      exposure:
        include: '*'
        exclude: threaddump,heapdump    
    jmx:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always
  info:
    git:
      mode: full
      
eureka:
  instance:
    metadata-map:
      user.name: admin
      user.password: password

info:
  contact:
    email: support@tacocloud.com
    phone: 822-625-6831